trigger:
  branches:
    include:
      - master  # Trigger on master branch push

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Checkout@1
  displayName: 'Checkout code'

- task: Docker@2
  displayName: 'Login to Docker Hub'
  inputs:
    command: 'login'
    containerRegistry: 'Dockerhubconnection'

- task: Docker@2
  displayName: 'Build and Push Docker Image'
  inputs:
    command: 'buildAndPush'
    repository: 'hiteshmechlin/timetracker'
    dockerfile: 'Dockerfile'  # Adjust this if your Dockerfile is located elsewhere
    tags: '$(Build.BuildId)'  # Tag with build ID or another version tag
    containerRegistry: 'Dockerhubconnection'
    buildContext: $(Build.SourcesDirectory)  # Path to your source directory
